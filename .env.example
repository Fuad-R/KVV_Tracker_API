# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=username
DB_PASSWORD=password
DB_SSLMODE=require

# Server Configuration
APP_PORT=8080

# Authentication (optional)
# Set AUTH=True to enable API key authentication on all endpoints
AUTH=False
API_KEY=your-api-key-here
